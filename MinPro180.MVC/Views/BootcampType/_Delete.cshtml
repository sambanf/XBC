@model MinPro180.ViewModel.BootcampTypeViewModel


<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.notes)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.notes)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.created_by)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.created_by)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.created_on)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.created_on)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.modified_by)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.modified_by)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.modified_on)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.modified_on)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.active)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.active)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "BootcampType", FormMethod.Post, new { id = "delete-form", name = "delete-form" }))
    {
        
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            <div class="form-actions no-color">
                <div class="btn-group btn-group-sm">
                    <input type="submit" value="Delete" class="btn btn-danger" />
                    <input type="button" value="Cancel" class="btn btn-default" data-dismiss="modal" />
                </div>

            </div>
        </div>
    }
</div>

    <script type="text/javascript">
        $("#delete-form").submit(function () {
            $.ajax({
                url: '@Url.Action("Delete","BootcampType")',
                type: this.method,
                data: $(this).serialize(),
                success: function (result) {
                    if (result.success) {
                        alert("Data Bootcamp Type Deleted");
                        $(".modal").modal("hide");
                        GetList();
                    } else {
                        alert("error " + result.message);
                    }
                }

            });
            return false;
        });
    </script>
    
